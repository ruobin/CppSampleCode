#include "HeapSort.h"

void Heapify(char *a, int count)
{
    /**

    for (start : count - 1 ~ 0)
    {

        SiftDown(a, start, count - 1)
        start --
    }
    **/


}

void SiftDown(char *a, int start, int end)
{

    /**
    root = start;
    if(leftChild(start) > a[root])
    {
        swap root with leftChild(start)
    }

    if(rightChild(start) > a[root])
    {
        swap root with rightChild
    }

    else
        return


    **/

}

HeapSort::HeapSort(char *words, int low, int high)
{
    //ctor
    /**



    **/
}

HeapSort::~HeapSort(char *words, int low, int high)
{
    //dtor
}
